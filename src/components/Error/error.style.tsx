import styled from 'styled-components';
import { colors } from '../../helpers/color';

export const ErrorStyled = styled.article`
    color:${colors.primary.Red};
    text-align: end;
    transition: all 0.5s;
    padding-right: 1%;
    font-size: 0.8em;
    padding-top: 4px;
`;